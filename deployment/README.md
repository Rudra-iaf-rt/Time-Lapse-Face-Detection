# Deployment overview

See `docs/DEPLOYMENT.md` and `deployment/recovery/README.md`.

Prometheus scrape example: `deployment/prometheus/prometheus.yml`.
Grafana: dashboards not fabricated — see `deployment/grafana/README.md`.
