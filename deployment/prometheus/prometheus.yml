# Prometheus scrape config for local/dev monitoring
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: multicam-api
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8000"]
